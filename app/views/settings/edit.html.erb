<div class="max-w-2xl mx-auto">
  <h1 class="text-2xl font-bold text-gray-900 mb-8">Settings</h1>

  <%= form_with url: settings_path, method: :patch, data: { controller: "settings-form" } do |f| %>
    <div class="bg-white border border-gh-border rounded-xl p-6 mb-6 shadow-sm">
      <h2 class="text-lg font-semibold text-gray-900 mb-1">LLM Provider per Feature</h2>
      <p class="text-xs text-gray-500 mb-5">Choose which provider to use for each AI feature.</p>

      <div class="space-y-4">
        <% feature_labels = {
          "parsing" => "Job Parsing",
          "skills_analysis" => "Skills Analysis",
          "match_score" => "Match Score",
          "cover_letter" => "Cover Letter",
          "project_recommendations" => "Project Recommendations",
          "experience_tailoring" => "Experience Writer"
        } %>
        <% @feature_providers.each do |feature, provider| %>
          <div class="border border-gh-border rounded-lg p-3 space-y-3">
            <div class="flex items-center justify-between">
              <label class="text-sm text-gray-700 font-medium"><%= feature_labels[feature] %></label>
              <select name="providers[<%= feature %>]"
                data-action="change->settings-form#toggle"
                class="bg-white border border-gray-300 rounded-lg px-3 py-1.5 text-sm text-gray-900 focus:outline-none focus:border-gh-green focus:ring-1 focus:ring-gh-green/30">
                <% LlmService::PROVIDERS.each do |p| %>
                  <option value="<%= p %>" <%= "selected" if provider == p %>><%= p.capitalize %></option>
                <% end %>
              </select>
            </div>

            <div>
              <label class="block text-xs text-gray-500 mb-1">Model</label>
              <input type="text"
                name="models[<%= feature %>]"
                value="<%= @feature_models[feature] %>"
                class="w-full bg-white border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-400 focus:outline-none focus:border-gh-green focus:ring-1 focus:ring-gh-green/30">
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="bg-white border border-gh-border rounded-xl p-6 mb-6 shadow-sm">
      <h2 class="text-lg font-semibold text-gray-900 mb-1">OpenRouter Configuration</h2>
      <p class="text-xs text-gray-500 mb-5">Used when a feature is set to OpenRouter.</p>

      <div>
        <label for="openrouter_api_key" class="block text-sm font-medium text-gray-700 mb-1">API Key</label>
        <input type="password" name="openrouter_api_key" id="openrouter_api_key"
          placeholder="sk-or-..."
          class="w-full bg-white border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-400 focus:outline-none focus:border-gh-green focus:ring-1 focus:ring-gh-green/30">
        <p class="text-xs text-gray-500 mt-1">
          <%= @openrouter_api_key_present ? "A key is already configured. Leave blank to keep it." : "No key configured yet." %>
        </p>
      </div>
    </div>

    <div class="bg-white border border-gh-border rounded-xl p-6 mb-6 shadow-sm <%= 'hidden' unless @feature_providers.values.include?('ollama') %>"
      data-settings-form-target="ollamaSettings">
      <h2 class="text-lg font-semibold text-gray-900 mb-4">Ollama Configuration</h2>

      <div class="space-y-4">
        <div>
          <label for="ollama_base_url" class="block text-sm font-medium text-gray-700 mb-1">Base URL</label>
          <input type="text" name="ollama_base_url" id="ollama_base_url"
            value="<%= @ollama_base_url %>"
            placeholder="http://localhost:11434"
            class="w-full bg-white border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-400 focus:outline-none focus:border-gh-green focus:ring-1 focus:ring-gh-green/30">
          <p class="text-xs text-gray-500 mt-1">The URL where Ollama is running (default: http://localhost:11434)</p>
        </div>

        <div>
          <label for="ollama_model" class="block text-sm font-medium text-gray-700 mb-1">Model</label>
          <input type="text" name="ollama_model" id="ollama_model"
            value="<%= @ollama_model %>"
            placeholder="glm4"
            class="w-full bg-white border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-400 focus:outline-none focus:border-gh-green focus:ring-1 focus:ring-gh-green/30">
          <p class="text-xs text-gray-500 mt-1">Global default for Ollama features. Per-feature Model values above override this.</p>
        </div>
      </div>
    </div>

    <div class="flex justify-end">
      <button type="submit" class="bg-gh-green hover:bg-gh-green-light text-white text-sm font-semibold px-6 py-2 rounded-lg transition-colors">
        Save Settings
      </button>
    </div>
  <% end %>
</div>

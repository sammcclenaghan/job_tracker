<div class="max-w-7xl mx-auto">
  <%# Back link + actions bar %>
  <div class="flex items-center justify-between mb-6">
    <%= link_to "← Applications", root_path, class: "text-gray-500 hover:text-gray-700 text-sm font-medium transition-colors" %>
    <div class="flex items-center gap-2">
      <%= form_with url: regenerate_insights_job_application_path(@job_application), method: :post, class: "inline" do %>
        <button type="submit" class="text-xs font-semibold px-3 py-1.5 rounded-lg bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 transition-colors inline-flex items-center gap-1.5 shadow-sm">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path></svg>
          Regenerate
        </button>
      <% end %>
      <%= link_to "Edit", edit_job_application_path(@job_application), class: "text-xs font-semibold px-3 py-1.5 rounded-lg bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 transition-colors shadow-sm" %>
      <%= button_to "Delete", job_application_path(@job_application), method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "text-xs font-semibold px-3 py-1.5 rounded-lg bg-white border border-red-300 hover:bg-red-50 text-red-600 transition-colors shadow-sm" %>
    </div>
  </div>

  <%# Insights progress banner %>
  <% if @job_application.insights_processing? %>
    <% progress = @job_application.insights_progress %>
    <div class="mb-6 px-4 py-3 rounded-lg bg-emerald-50 border border-emerald-200 flex items-center gap-3">
      <svg class="animate-spin h-4 w-4 text-gh-green shrink-0" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
      </svg>
      <p class="text-sm text-emerald-800">Generating insights... <span class="font-semibold"><%= progress[:completed] %>/<%= progress[:total] %></span> complete. Refresh to see updates.</p>
    </div>
  <% end %>

  <%# Provider error %>
  <% if @job_application.provider_error.present? %>
    <div class="mb-6 px-4 py-3 rounded-lg bg-red-50 border border-red-200 flex items-start gap-3">
      <svg class="w-4 h-4 text-red-500 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z"></path>
      </svg>
      <p class="text-sm text-red-700"><%= @job_application.provider_error %></p>
    </div>
  <% end %>

  <%# Two-column layout %>
  <div class="grid grid-cols-1 lg:grid-cols-12 gap-6">

    <%# LEFT SIDEBAR — Job overview (sticky) %>
    <div class="lg:col-span-4 space-y-4">
      <div class="lg:sticky lg:top-20 space-y-4">

        <%# Title + Score card %>
        <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
          <div class="flex items-start justify-between gap-3 mb-3">
            <div class="min-w-0">
              <h1 class="text-xl font-bold text-gray-900 leading-tight"><%= @job_application.job_title %></h1>
              <p class="text-gray-500 mt-1"><%= @job_application.company_name %></p>
            </div>
            <% if @job_application.match_score.present? %>
              <% score = @job_application.match_score %>
              <% score_color = case score
                   when 80..100 then "text-emerald-600 border-emerald-200 bg-emerald-50"
                   when 60..79 then "text-blue-600 border-blue-200 bg-blue-50"
                   when 40..59 then "text-amber-600 border-amber-200 bg-amber-50"
                   else "text-red-600 border-red-200 bg-red-50"
                 end %>
              <div class="<%= score_color %> border rounded-xl px-3 py-2 text-center shrink-0">
                <div class="text-2xl font-bold leading-none"><%= score %></div>
                <div class="text-[10px] uppercase tracking-wider mt-0.5 opacity-70">match</div>
              </div>
            <% end %>
          </div>

          <% if @job_application.match_score_reasoning.present? %>
            <p class="text-xs text-gray-500 leading-relaxed mb-4"><%= @job_application.match_score_reasoning %></p>
          <% end %>

          <%# Status pills %>
          <div data-controller="status">
            <div class="flex flex-wrap gap-1.5">
              <% JobApplication::STATUSES.each do |status| %>
                <% is_current = status == @job_application.status %>
                <button
                  type="button"
                  data-status-target="button"
                  data-status="<%= status %>"
                  data-url="<%= update_status_job_application_path(@job_application, status: status) %>"
                  data-action="click->status#select"
                  class="text-[11px] font-semibold px-2.5 py-1 rounded-full border transition-colors cursor-pointer <%= is_current ? 'bg-gh-green border-gh-green text-white' : 'border-gray-300 text-gray-500 hover:bg-gray-100 hover:text-gray-700' %>"
                >
                  <%= status.capitalize %>
                </button>
              <% end %>
            </div>
          </div>
        </div>

        <%# Details card %>
        <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
          <dl class="space-y-3 text-sm">
            <% if @job_application.location.present? %>
              <div class="flex items-center gap-2.5">
                <svg class="w-4 h-4 text-gray-400 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path></svg>
                <span class="text-gray-700"><%= @job_application.location %></span>
                <% if @job_application.work_arrangement.present? %>
                  <span class="text-[10px] uppercase tracking-wider text-gray-500 bg-gray-100 px-1.5 py-0.5 rounded"><%= @job_application.work_arrangement %></span>
                <% end %>
              </div>
            <% end %>
            <% if @job_application.salary_range.present? %>
              <div class="flex items-center gap-2.5">
                <svg class="w-4 h-4 text-gray-400 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                <span class="text-gray-700"><%= @job_application.salary_range %></span>
              </div>
            <% end %>
            <% if @job_application.contact_email.present? %>
              <div class="flex items-center gap-2.5">
                <svg class="w-4 h-4 text-gray-400 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                <span class="text-gray-700 truncate"><%= @job_application.contact_email %></span>
              </div>
            <% end %>
            <% if @job_application.job_url.present? %>
              <div class="flex items-center gap-2.5">
                <svg class="w-4 h-4 text-gray-400 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path></svg>
                <%= link_to "View posting →", @job_application.job_url, target: "_blank", class: "text-gh-green hover:text-gh-green-light transition-colors font-medium" %>
              </div>
            <% end %>
          </dl>

          <div class="border-t border-gh-border mt-4 pt-4">
            <dl class="grid grid-cols-2 gap-3 text-xs">
              <div>
                <dt class="text-gray-400">Saved</dt>
                <dd class="text-gray-600 mt-0.5"><%= @job_application.created_at.strftime("%b %d, %Y") %></dd>
              </div>
              <div>
                <dt class="text-gray-400">Applied</dt>
                <dd class="text-gray-600 mt-0.5"><%= @job_application.applied_at&.strftime("%b %d, %Y") || "--" %></dd>
              </div>
            </dl>
          </div>
        </div>

        <%# Skills card %>
        <% if @job_application.skills_list.any? %>
          <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
            <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">Skills Match</h3>
            <div class="flex flex-wrap gap-1.5">
              <% @job_application.skills_list.each do |skill| %>
                <% s = @job_application.skill_status(skill) %>
                <% pill = case s
                     when :matching then "bg-emerald-50 text-emerald-700 border-emerald-200"
                     when :missing then "bg-red-50 text-red-600 border-red-200"
                     else "bg-gray-50 text-gray-600 border-gray-200"
                   end %>
                <span class="text-[11px] font-medium px-2 py-0.5 rounded-full border <%= pill %>"><%= skill %></span>
              <% end %>
            </div>
            <% if @job_application.has_skills_analysis? %>
              <div class="flex items-center gap-3 mt-3 text-xs">
                <span class="flex items-center gap-1 text-emerald-600">
                  <span class="w-1.5 h-1.5 rounded-full bg-emerald-500"></span>
                  <%= @job_application.matching_skills.size %> matched
                </span>
                <span class="flex items-center gap-1 text-red-500">
                  <span class="w-1.5 h-1.5 rounded-full bg-red-500"></span>
                  <%= @job_application.missing_skills.size %> gaps
                </span>
              </div>
            <% end %>
          </div>
        <% end %>

      </div>
    </div>

    <%# RIGHT MAIN CONTENT — Tabbed insights %>
    <div class="lg:col-span-8" data-controller="tabs">

      <%# Tab navigation %>
      <div class="border-b border-gh-border mb-6">
        <nav class="flex gap-6 -mb-px">
          <button data-tabs-target="tab" data-tabs-id="cover-letter" data-action="click->tabs#select"
            class="pb-3 text-sm font-medium border-b-2 transition-colors cursor-pointer">
            Cover Letter
          </button>
          <button data-tabs-target="tab" data-tabs-id="resume" data-action="click->tabs#select"
            class="pb-3 text-sm font-medium border-b-2 transition-colors cursor-pointer">
            Experience Writer
          </button>
          <button data-tabs-target="tab" data-tabs-id="projects" data-action="click->tabs#select"
            class="pb-3 text-sm font-medium border-b-2 transition-colors cursor-pointer">
            Projects
          </button>
          <button data-tabs-target="tab" data-tabs-id="details" data-action="click->tabs#select"
            class="pb-3 text-sm font-medium border-b-2 transition-colors cursor-pointer">
            Job Details
          </button>
        </nav>
      </div>

      <%# ==================== COVER LETTER TAB ==================== %>
      <div data-tabs-target="panel" data-tabs-id="cover-letter" data-controller="clipboard regenerate-modal" data-action="keydown@document->regenerate-modal#closeOnEscape">
        <div class="flex items-center justify-between mb-4">
          <h2 class="text-lg font-semibold text-gray-900">Cover Letter</h2>
          <div class="flex items-center gap-2">
            <% if @job_application.cover_letter.present? %>
              <button type="button" data-clipboard-target="button" data-action="click->clipboard#copy"
                class="text-xs font-semibold px-3 py-1.5 rounded-lg bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 transition-colors shadow-sm">
                Copy
              </button>
              <button type="button" data-action="click->regenerate-modal#open"
                class="text-xs font-semibold px-3 py-1.5 rounded-lg bg-gh-green hover:bg-gh-green-light text-white transition-colors shadow-sm">
                Regenerate
              </button>
            <% end %>
          </div>
        </div>

        <% if @job_application.cover_letter.present? %>
          <div class="bg-white border border-gh-border rounded-xl p-6 shadow-sm">
            <div data-clipboard-target="source" class="text-gray-700 text-sm leading-relaxed whitespace-pre-wrap"><%= @job_application.cover_letter %></div>
          </div>
        <% else %>
          <div class="bg-white border border-gh-border rounded-xl p-12 text-center shadow-sm">
            <p class="text-gray-500 text-sm mb-3">Cover letters are generated manually.</p>
            <%= form_with url: regenerate_cover_letter_job_application_path(@job_application), method: :post, class: "inline" do %>
              <button type="submit" class="text-xs font-semibold px-4 py-2 rounded-lg bg-gh-green hover:bg-gh-green-light text-white transition-colors">Generate Cover Letter</button>
            <% end %>
          </div>
        <% end %>

        <%# Regenerate modal %>
        <div data-regenerate-modal-target="modal" data-action="click->regenerate-modal#closeOnBackdrop" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/40">
          <div class="bg-white border border-gh-border rounded-xl shadow-2xl w-full max-w-md mx-4 p-6">
            <div class="flex items-center justify-between mb-4">
              <h3 class="text-base font-semibold text-gray-900">Regenerate Cover Letter</h3>
              <button type="button" data-action="click->regenerate-modal#close" class="text-gray-400 hover:text-gray-600 transition-colors">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
              </button>
            </div>
            <p class="text-sm text-gray-500 mb-4">What should be different?</p>
            <%= form_with url: regenerate_cover_letter_job_application_path(@job_application), method: :post, data: { action: "submit->regenerate-modal#submit" } do |f| %>
              <textarea name="feedback" data-regenerate-modal-target="feedback" rows="3"
                placeholder="Emphasize my Redis and background job experience more..."
                class="w-full bg-white border border-gray-300 rounded-lg px-3 py-2 text-sm text-gray-900 placeholder-gray-400 focus:outline-none focus:border-gh-green focus:ring-1 focus:ring-gh-green/30 resize-none mb-4"></textarea>
              <div class="flex justify-end gap-3">
                <button type="button" data-action="click->regenerate-modal#close" class="text-xs font-semibold px-4 py-2 rounded-lg bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 transition-colors">Cancel</button>
                <button type="submit" data-regenerate-modal-target="button" class="text-xs font-semibold px-4 py-2 rounded-lg bg-gh-green hover:bg-gh-green-light text-white transition-colors inline-flex items-center gap-2">
                  <svg data-regenerate-modal-target="spinner" class="hidden animate-spin h-3 w-3" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                    <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                  </svg>
                  <span data-regenerate-modal-target="text">Regenerate</span>
                </button>
              </div>
            <% end %>
          </div>
        </div>
      </div>

      <%# ==================== EXPERIENCE WRITER TAB ==================== %>
      <div data-tabs-target="panel" data-tabs-id="resume" class="hidden">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Experience Writer</h2>

        <%# Experience Tailoring %>
        <% if @job_application.has_experience_tailoring? %>
          <div class="mb-6">
            <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">Tailored Bullets (Google X-Y-Z)</h3>
            <div class="space-y-4">
              <% @job_application.tailored_jobs.each do |job| %>
                <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
                  <div class="flex items-baseline gap-2 mb-4">
                    <h4 class="text-sm font-semibold text-gray-900"><%= job["title"] %></h4>
                    <span class="text-xs text-gray-400">· <%= job["company"] %></span>
                  </div>
                  <div class="space-y-3">
                    <% (job["bullets"] || []).each do |bullet| %>
                      <div class="space-y-1.5">
                        <div class="flex items-start gap-2">
                          <span class="text-red-400 text-xs mt-0.5 shrink-0 font-mono">-</span>
                          <span class="text-xs text-gray-400 line-through leading-relaxed"><%= bullet["original"] %></span>
                        </div>
                        <div class="flex items-start gap-2">
                          <span class="text-emerald-500 text-xs mt-0.5 shrink-0 font-mono">+</span>
                          <span class="text-xs text-emerald-700 leading-relaxed font-medium"><%= bullet["tailored"] %></span>
                        </div>
                        <% if bullet["change_summary"].present? %>
                          <p class="text-[11px] text-gray-400 pl-5"><%= bullet["change_summary"] %></p>
                        <% end %>
                      </div>
                    <% end %>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        <% else %>
          <div class="bg-white border border-gh-border rounded-xl p-12 text-center shadow-sm">
            <% if @job_application.insights_processing? %>
              <svg class="animate-spin h-6 w-6 text-gray-300 mx-auto mb-3" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
              </svg>
              <p class="text-gray-400 text-sm">Writing experience bullets...</p>
            <% else %>
              <p class="text-gray-400 text-sm">No experience rewrites yet. Click "Regenerate" to generate tailored bullets.</p>
            <% end %>
          </div>
        <% end %>
      </div>

      <%# ==================== PROJECTS TAB ==================== %>
      <div data-tabs-target="panel" data-tabs-id="projects" class="hidden">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Project Strategy</h2>

        <% if @job_application.has_project_recommendations? %>
          <div class="space-y-4">
            <%# Highlight %>
            <% if @job_application.highlighted_projects.any? %>
              <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
                <div class="flex items-center gap-2 mb-4">
                  <span class="w-2 h-2 rounded-full bg-emerald-500"></span>
                  <h3 class="text-sm font-semibold text-gray-900">Feature These</h3>
                </div>
                <div class="space-y-4">
                  <% @job_application.highlighted_projects.each do |project| %>
                    <div>
                      <p class="text-sm font-medium text-emerald-700"><%= project["project"] %></p>
                      <p class="text-xs text-gray-500 mt-1 leading-relaxed"><%= project["reason"] %></p>
                      <% if project["resume_bullets"].present? %>
                        <div class="mt-2">
                          <p class="text-[11px] text-gray-400 uppercase tracking-wide mb-1">Resume Bullets</p>
                          <ul class="space-y-1">
                            <% project["resume_bullets"].first(2).each do |bullet| %>
                              <li class="text-xs text-emerald-800 leading-relaxed pl-3 border-l-2 border-emerald-200">• <%= bullet %></li>
                            <% end %>
                          </ul>
                        </div>
                      <% end %>
                      <% if project["talking_points"].present? %>
                        <div class="mt-2 space-y-1">
                          <% project["talking_points"].each do |point| %>
                            <p class="text-xs text-gray-500 pl-3 border-l-2 border-gray-200"><%= point %></p>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  <% end %>
                </div>
              </div>
            <% end %>

            <%# Deprioritize %>
            <% if @job_application.deprioritized_projects.any? %>
              <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
                <div class="flex items-center gap-2 mb-4">
                  <span class="w-2 h-2 rounded-full bg-amber-500"></span>
                  <h3 class="text-sm font-semibold text-gray-900">Deprioritize</h3>
                </div>
                <div class="space-y-4">
                  <% @job_application.deprioritized_projects.each do |project| %>
                    <div>
                      <p class="text-sm font-medium text-amber-700"><%= project["project"] %></p>
                      <p class="text-xs text-gray-500 mt-1 leading-relaxed"><%= project["reason"] %></p>
                      <% if project["resume_bullets"].present? %>
                        <div class="mt-2">
                          <p class="text-[11px] text-gray-400 uppercase tracking-wide mb-1">If Kept, Rewrite As</p>
                          <ul class="space-y-1">
                            <% project["resume_bullets"].first(2).each do |bullet| %>
                              <li class="text-xs text-amber-800 leading-relaxed pl-3 border-l-2 border-amber-200">• <%= bullet %></li>
                            <% end %>
                          </ul>
                        </div>
                      <% end %>
                      <% if project["alternative"].present? %>
                        <p class="text-xs text-gray-500 mt-1">→ <%= project["alternative"] %></p>
                      <% end %>
                    </div>
                  <% end %>
                </div>
              </div>
            <% end %>

            <%# Missing / Build %>
            <% if @job_application.missing_projects.any? %>
              <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
                <div class="flex items-center gap-2 mb-4">
                  <span class="w-2 h-2 rounded-full bg-blue-500"></span>
                  <h3 class="text-sm font-semibold text-gray-900">Projects to Build</h3>
                </div>
                <div class="space-y-4">
                  <% @job_application.missing_projects.each do |project| %>
                    <div>
                      <p class="text-sm font-medium text-blue-700"><%= project["idea"] %></p>
                      <p class="text-xs text-gray-500 mt-1 leading-relaxed"><%= project["reason"] %></p>
                      <% if project["resume_bullets"].present? %>
                        <div class="mt-2">
                          <p class="text-[11px] text-gray-400 uppercase tracking-wide mb-1">Resume Bullets</p>
                          <ul class="space-y-1">
                            <% project["resume_bullets"].first(2).each do |bullet| %>
                              <li class="text-xs text-blue-800 leading-relaxed pl-3 border-l-2 border-blue-200">• <%= bullet %></li>
                            <% end %>
                          </ul>
                        </div>
                      <% end %>
                      <% if project["skills_demonstrated"].present? %>
                        <div class="flex flex-wrap gap-1 mt-2">
                          <% project["skills_demonstrated"].each do |skill| %>
                            <span class="text-[10px] bg-blue-50 text-blue-600 border border-blue-200 px-1.5 py-0.5 rounded-full"><%= skill %></span>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="bg-white border border-gh-border rounded-xl p-12 text-center shadow-sm">
            <% if @job_application.insights_processing? %>
              <svg class="animate-spin h-6 w-6 text-gray-300 mx-auto mb-3" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
              </svg>
              <p class="text-gray-400 text-sm">Analyzing your projects...</p>
            <% else %>
              <p class="text-gray-400 text-sm">No project recommendations yet. Click "Regenerate" to get advice.</p>
            <% end %>
          </div>
        <% end %>
      </div>

      <%# ==================== JOB DETAILS TAB ==================== %>
      <div data-tabs-target="panel" data-tabs-id="details" class="hidden">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Job Details</h2>
        <div class="space-y-4">
          <% if @job_application.job_description.present? %>
            <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
              <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">Description</h3>
              <div class="text-gray-700 text-sm leading-relaxed whitespace-pre-wrap"><%= @job_application.job_description %></div>
            </div>
          <% end %>

          <% if @job_application.application_instructions.present? %>
            <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
              <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">How to Apply</h3>
              <div class="text-gray-700 text-sm leading-relaxed whitespace-pre-wrap"><%= @job_application.application_instructions %></div>
            </div>
          <% end %>

          <% if @job_application.notes.present? %>
            <div class="bg-white border border-gh-border rounded-xl p-5 shadow-sm">
              <h3 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">Notes</h3>
              <div class="text-gray-700 text-sm leading-relaxed whitespace-pre-wrap"><%= @job_application.notes %></div>
            </div>
          <% end %>

          <% if @job_application.job_description.blank? && @job_application.notes.blank? %>
            <div class="bg-white border border-gh-border rounded-xl p-12 text-center shadow-sm">
              <p class="text-gray-400 text-sm">No details available. <%= link_to "Edit this application", edit_job_application_path(@job_application), class: "text-gh-green hover:text-gh-green-light font-medium" %> to add them.</p>
            </div>
          <% end %>
        </div>
      </div>

    </div>
  </div>
</div>
